align_raw_station_name <- function(rawStationName){
  # TODO: map the rawStationName to a standard name from Station_Mean_Coords.csv

  # For now, just return the input name (no changes)
  return(rawStationName)
}