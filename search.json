[
  {
    "objectID": "cruise_reports/cruise_reports.html",
    "href": "cruise_reports/cruise_reports.html",
    "title": "Cruise Reports",
    "section": "",
    "text": "SV18067\n\n\n\n\n\n\n\n\n\n\nSV18173\n\n\n\n\n\n\n\n\n\n\nWB22215\n\n\n\n\n\n\n\n\n\n\nWS0603\n\n\n\n\n\n\n\n\n\n\nWS0612\n\n\n\n\n\n\n\n\n\n\nWS0618\n\n\n\n\n\n\n\n\n\n\nWS0623\n\n\n\n\n\n\n\n\n\n\nWS0704\n\n\n\n\n\n\n\n\n\n\nWS0718\n\n\n\n\n\n\n\n\n\n\nWS0802\n\n\n\n\n\n\n\n\n\n\nWS0807\n\n\n\n\n\n\n\n\n\n\nWS0901\n\n\n\n\n\n\n\n\n\n\nWS0906\n\n\n\n\n\n\n\n\n\n\nWS0914\n\n\n\n\n\n\n\n\n\n\nWS0919\n\n\n\n\n\n\n\n\n\n\nWS0923\n\n\n\n\n\n\n\n\n\n\nWS1004\n\n\n\n\n\n\n\n\n\n\nWS1007\n\n\n\n\n\n\n\n\n\n\nWS1015\n\n\n\n\n\n\n\n\n\n\nWS1018\n\n\n\n\n\n\n\n\n\n\nWS1102\n\n\n\n\n\n\n\n\n\n\nWS1106\n\n\n\n\n\n\n\n\n\n\nWS1113\n\n\n\n\n\n\n\n\n\n\nWS1116\n\n\n\n\n\n\n\n\n\n\nWS1202\n\n\n\n\n\n\n\n\n\n\nWS1207\n\n\n\n\n\n\n\n\n\n\nWS1212\n\n\n\n\n\n\n\n\n\n\nWS14335\n\n\n\n\n\n\n\n\n\n\nWS15103\n\n\n\n\n\n\n\n\n\n\nWS15148\n\n\n\n\n\n\n\n\n\n\nWS15208\n\n\n\n\n\n\n\n\n\n\nWS15264\n\n\n\n\n\n\n\n\n\n\nWS15320\n\n\n\n\n\n\n\n\n\n\nWS16004\n\n\n\n\n\n\n\n\n\n\nWS16074\n\n\n\n\n\n\n\n\n\n\nWS16207\n\n\n\n\n\n\n\n\n\n\nWS16263\n\n\n\n\n\n\n\n\n\n\nWS16319\n\n\n\n\n\n\n\n\n\n\nWS17030\n\n\n\n\n\n\n\n\n\n\nWS17086\n\n\n\n\n\n\n\n\n\n\nWS17170\n\n\n\n\n\n\n\n\n\n\nWS17212\n\n\n\n\n\n\n\n\n\n\nWS17282\n\n\n\n\n\n\n\n\n\n\nWS18008\n\n\n\n\n\n\n\n\n\n\nWS18120\n\n\n\n\n\n\n\n\n\n\nWS18218\n\n\n\n\n\n\n\n\n\n\nWS18285\n\n\n\n\n\n\n\n\n\n\nWS18351\n\n\n\n\n\n\n\n\n\n\nWS19028\n\n\n\n\n\n\n\n\n\n\nWS19119\n\n\n\n\n\n\n\n\n\n\nWS19210\n\n\n\n\n\n\n\n\n\n\nWS19266\n\n\n\n\n\n\n\n\n\n\nWS19322\n\n\n\n\n\n\n\n\n\n\nWS20006\n\n\n\n\n\n\n\n\n\n\nWS20231\n\n\n\n\n\n\n\n\n\n\nWS20279\n\n\n\n\n\n\n\n\n\n\nWS20342\n\n\n\n\n\n\n\n\n\n\nWS21032\n\n\n\n\n\n\n\n\n\n\nWS21093\n\n\n\n\n\n\n\n\n\n\nWS21151\n\n\n\n\n\n\n\n\n\n\nWS21212\n\n\n\n\n\n\n\n\n\n\nWS21278\n\n\n\n\n\n\n\n\n\n\nWS21338\n\n\n\n\n\n\n\n\n\n\nWS22022\n\n\n\n\n\n\n\n\n\n\nWS22072\n\n\n\n\n\n\n\n\n\n\nWS22141\n\n\n\n\n\n\n\n\n\n\nWS22281\n\n\n\n\n\n\n\n\n\n\nWS22337\n\n\n\n\n\n\n\n\n\n\nWS23011\n\n\n\n\n\n\n\n\n\n\nWS23061\n\n\n\n\n\n\n\n\n\n\nWS23203\n\n\n\n\n\n\n\n\n\n\nWS24139\n\n\n\n\n\nNo matching items"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS23203.html",
    "href": "cruise_reports/cruise_reports/WS23203.html",
    "title": "WS23203",
    "section": "",
    "text": "WS23203\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 84 files for cruise WS23203 \nTotal rows: 6930 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS23011.html",
    "href": "cruise_reports/cruise_reports/WS23011.html",
    "title": "WS23011",
    "section": "",
    "text": "WS23011\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 71 files for cruise WS23011 \nTotal rows: 4330 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS22281.html",
    "href": "cruise_reports/cruise_reports/WS22281.html",
    "title": "WS22281",
    "section": "",
    "text": "WS22281\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 73 files for cruise WS22281 \nTotal rows: 5350 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS22072.html",
    "href": "cruise_reports/cruise_reports/WS22072.html",
    "title": "WS22072",
    "section": "",
    "text": "WS22072\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 75 files for cruise WS22072 \nTotal rows: 6296 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS21338.html",
    "href": "cruise_reports/cruise_reports/WS21338.html",
    "title": "WS21338",
    "section": "",
    "text": "WS21338\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 69 files for cruise WS21338 \nTotal rows: 5940 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS21212.html",
    "href": "cruise_reports/cruise_reports/WS21212.html",
    "title": "WS21212",
    "section": "",
    "text": "WS21212\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 71 files for cruise WS21212 \nTotal rows: 4232 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS21093.html",
    "href": "cruise_reports/cruise_reports/WS21093.html",
    "title": "WS21093",
    "section": "",
    "text": "WS21093\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 63 files for cruise WS21093 \nTotal rows: 3977 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS20342.html",
    "href": "cruise_reports/cruise_reports/WS20342.html",
    "title": "WS20342",
    "section": "",
    "text": "WS20342\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 67 files for cruise WS20342 \nTotal rows: 4417 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS20231.html",
    "href": "cruise_reports/cruise_reports/WS20231.html",
    "title": "WS20231",
    "section": "",
    "text": "WS20231\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 44 files for cruise WS20231 \nTotal rows: 3013 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS19322.html",
    "href": "cruise_reports/cruise_reports/WS19322.html",
    "title": "WS19322",
    "section": "",
    "text": "WS19322\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 71 files for cruise WS19322 \nTotal rows: 4462 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS19210.html",
    "href": "cruise_reports/cruise_reports/WS19210.html",
    "title": "WS19210",
    "section": "",
    "text": "WS19210\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 42 files for cruise WS19210 \nTotal rows: 2453 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS19028.html",
    "href": "cruise_reports/cruise_reports/WS19028.html",
    "title": "WS19028",
    "section": "",
    "text": "WS19028\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 35 files for cruise WS19028 \nTotal rows: 1346 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS18285.html",
    "href": "cruise_reports/cruise_reports/WS18285.html",
    "title": "WS18285",
    "section": "",
    "text": "WS18285\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 61 files for cruise WS18285 \nTotal rows: 3647 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS18120.html",
    "href": "cruise_reports/cruise_reports/WS18120.html",
    "title": "WS18120",
    "section": "",
    "text": "WS18120\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 20 files for cruise WS18120 \nTotal rows: 925 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS17282.html",
    "href": "cruise_reports/cruise_reports/WS17282.html",
    "title": "WS17282",
    "section": "",
    "text": "WS17282\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 62 files for cruise WS17282 \nTotal rows: 4649 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS17170.html",
    "href": "cruise_reports/cruise_reports/WS17170.html",
    "title": "WS17170",
    "section": "",
    "text": "WS17170\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 22 files for cruise WS17170 \nTotal rows: 445 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS17030.html",
    "href": "cruise_reports/cruise_reports/WS17030.html",
    "title": "WS17030",
    "section": "",
    "text": "WS17030\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 25 files for cruise WS17030 \nTotal rows: 1818 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS16263.html",
    "href": "cruise_reports/cruise_reports/WS16263.html",
    "title": "WS16263",
    "section": "",
    "text": "WS16263\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 28 files for cruise WS16263 \nTotal rows: 1536 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS16074.html",
    "href": "cruise_reports/cruise_reports/WS16074.html",
    "title": "WS16074",
    "section": "",
    "text": "WS16074\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 26 files for cruise WS16074 \nTotal rows: 1042 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS15320.html",
    "href": "cruise_reports/cruise_reports/WS15320.html",
    "title": "WS15320",
    "section": "",
    "text": "WS15320\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 23 files for cruise WS15320 \nTotal rows: 1436 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS15208.html",
    "href": "cruise_reports/cruise_reports/WS15208.html",
    "title": "WS15208",
    "section": "",
    "text": "WS15208\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 40 files for cruise WS15208 \nTotal rows: 7577 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS15103.html",
    "href": "cruise_reports/cruise_reports/WS15103.html",
    "title": "WS15103",
    "section": "",
    "text": "WS15103\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 26 files for cruise WS15103 \nTotal rows: 1578 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS1212.html",
    "href": "cruise_reports/cruise_reports/WS1212.html",
    "title": "WS1212",
    "section": "",
    "text": "WS1212\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 35 files for cruise WS1212 \nTotal rows: 4911 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS1202.html",
    "href": "cruise_reports/cruise_reports/WS1202.html",
    "title": "WS1202",
    "section": "",
    "text": "WS1202\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 34 files for cruise WS1202 \nTotal rows: 4951 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS1113.html",
    "href": "cruise_reports/cruise_reports/WS1113.html",
    "title": "WS1113",
    "section": "",
    "text": "WS1113\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 40 files for cruise WS1113 \nTotal rows: 10048 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS1102.html",
    "href": "cruise_reports/cruise_reports/WS1102.html",
    "title": "WS1102",
    "section": "",
    "text": "WS1102\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 34 files for cruise WS1102 \nTotal rows: 5992 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS1015.html",
    "href": "cruise_reports/cruise_reports/WS1015.html",
    "title": "WS1015",
    "section": "",
    "text": "WS1015\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 47 files for cruise WS1015 \nTotal rows: 4737 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS1004.html",
    "href": "cruise_reports/cruise_reports/WS1004.html",
    "title": "WS1004",
    "section": "",
    "text": "WS1004\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 33 files for cruise WS1004 \nTotal rows: 3024 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS0919.html",
    "href": "cruise_reports/cruise_reports/WS0919.html",
    "title": "WS0919",
    "section": "",
    "text": "WS0919\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 27 files for cruise WS0919 \nTotal rows: 2992 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS0906.html",
    "href": "cruise_reports/cruise_reports/WS0906.html",
    "title": "WS0906",
    "section": "",
    "text": "WS0906\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 27 files for cruise WS0906 \nTotal rows: 3708 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS0807.html",
    "href": "cruise_reports/cruise_reports/WS0807.html",
    "title": "WS0807",
    "section": "",
    "text": "WS0807\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 38 files for cruise WS0807 \nTotal rows: 5520 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS0718.html",
    "href": "cruise_reports/cruise_reports/WS0718.html",
    "title": "WS0718",
    "section": "",
    "text": "WS0718\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 38 files for cruise WS0718 \nTotal rows: 5124 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS0623.html",
    "href": "cruise_reports/cruise_reports/WS0623.html",
    "title": "WS0623",
    "section": "",
    "text": "WS0623\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 32 files for cruise WS0623 \nTotal rows: 1694 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS0612.html",
    "href": "cruise_reports/cruise_reports/WS0612.html",
    "title": "WS0612",
    "section": "",
    "text": "WS0612\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 91 files for cruise WS0612 \nTotal rows: 14495 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WB22215.html",
    "href": "cruise_reports/cruise_reports/WB22215.html",
    "title": "WB22215",
    "section": "",
    "text": "WB22215\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 62 files for cruise WB22215 \nTotal rows: 6009 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\nerror generating surface map for chlorophyll_fluorescence:\n&lt;error/rlang_error&gt;\nError in `summarise()`:\nℹ In argument: `mean_value = mean(.data[[\"chlorophyll_fluorescence\"]],\n  na.rm = TRUE)`.\nℹ In group 1: `station = \"12\"`.\nCaused by error:\n! error in evaluating the argument 'x' in selecting a method for function 'mean': Column `chlorophyll_fluorescence` not found in `.data`.\n---\nBacktrace:\n     ▆\n  1. ├─global createSurfaceMap(...)\n  2. │ ├─base::tryCatch(...)\n  3. │ │ └─base (local) tryCatchList(expr, classes, parentenv, handlers)\n  4. │ │   └─base (local) tryCatchOne(expr, names, parentenv, handlers[[1L]])\n  5. │ │     └─base (local) doTryCatch(return(expr), name, parentenv, handler)\n  6. │ └─... %&gt;% ...\n  7. ├─dplyr::summarise(...)\n  8. ├─dplyr:::summarise.grouped_df(...)\n  9. │ └─dplyr:::summarise_cols(.data, dplyr_quosures(...), by, \"summarise\")\n 10. │   ├─base::withCallingHandlers(...)\n 11. │   └─dplyr:::map(quosures, summarise_eval_one, mask = mask)\n 12. │     └─base::lapply(.x, .f, ...)\n 13. │       └─dplyr (local) FUN(X[[i]], ...)\n 14. │         └─mask$eval_all_summarise(quo)\n 15. │           └─dplyr (local) eval()\n 16. ├─base::mean(.data[[\"chlorophyll_fluorescence\"]], na.rm = TRUE)\n 17. ├─chlorophyll_fluorescence\n 18. ├─rlang:::`[[.rlang_data_pronoun`(.data, \"chlorophyll_fluorescence\")\n 19. │ └─rlang:::data_pronoun_get(...)\n 20. ├─rlang:::abort_data_pronoun(x, call = y)\n 21. │ └─rlang::abort(msg, \"rlang_error_data_pronoun_not_found\", call = call)\n 22. │   └─rlang:::signal_abort(cnd, .file)\n 23. │     └─base::signalCondition(cnd)\n 24. └─base (local) `&lt;fn&gt;`(`&lt;rln_____&gt;`)\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n  model        psill     range\n1   Nug 279.95965674   0.00000\n2   Exp   0.01233484 -67.56094\nerror generating surface map for sea_water_salinity:\n&lt;simpleError in load.variogram.model(object, c(0, 0)): variogram range can never be negative&gt;"
  },
  {
    "objectID": "cruise_reports/cruise_reports/SV18067.html",
    "href": "cruise_reports/cruise_reports/SV18067.html",
    "title": "SV18067",
    "section": "",
    "text": "SV18067\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 28 files for cruise SV18067 \nTotal rows: 2606 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\nerror generating surface map for chlorophyll_fluorescence:\n&lt;error/rlang_error&gt;\nError in `summarise()`:\nℹ In argument: `mean_value = mean(.data[[\"chlorophyll_fluorescence\"]],\n  na.rm = TRUE)`.\nℹ In group 1: `station = \"12\"`.\nCaused by error:\n! error in evaluating the argument 'x' in selecting a method for function 'mean': Column `chlorophyll_fluorescence` not found in `.data`.\n---\nBacktrace:\n     ▆\n  1. ├─global createSurfaceMap(...)\n  2. │ ├─base::tryCatch(...)\n  3. │ │ └─base (local) tryCatchList(expr, classes, parentenv, handlers)\n  4. │ │   └─base (local) tryCatchOne(expr, names, parentenv, handlers[[1L]])\n  5. │ │     └─base (local) doTryCatch(return(expr), name, parentenv, handler)\n  6. │ └─... %&gt;% ...\n  7. ├─dplyr::summarise(...)\n  8. ├─dplyr:::summarise.grouped_df(...)\n  9. │ └─dplyr:::summarise_cols(.data, dplyr_quosures(...), by, \"summarise\")\n 10. │   ├─base::withCallingHandlers(...)\n 11. │   └─dplyr:::map(quosures, summarise_eval_one, mask = mask)\n 12. │     └─base::lapply(.x, .f, ...)\n 13. │       └─dplyr (local) FUN(X[[i]], ...)\n 14. │         └─mask$eval_all_summarise(quo)\n 15. │           └─dplyr (local) eval()\n 16. ├─base::mean(.data[[\"chlorophyll_fluorescence\"]], na.rm = TRUE)\n 17. ├─chlorophyll_fluorescence\n 18. ├─rlang:::`[[.rlang_data_pronoun`(.data, \"chlorophyll_fluorescence\")\n 19. │ └─rlang:::data_pronoun_get(...)\n 20. ├─rlang:::abort_data_pronoun(x, call = y)\n 21. │ └─rlang::abort(msg, \"rlang_error_data_pronoun_not_found\", call = call)\n 22. │   └─rlang:::signal_abort(cnd, .file)\n 23. │     └─base::signalCondition(cnd)\n 24. └─base (local) `&lt;fn&gt;`(`&lt;rln_____&gt;`)\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "index.html",
    "href": "index.html",
    "title": "SEUS MBON CTD Ingestion & Processing",
    "section": "",
    "text": "This website outlines ingestion and processing of CTD data collected from the SEUS MBON Research Cruises in the South Florida region.\n“Raw” CTD casts are ingested from an ERDDAP server and processed using the OCE R library."
  },
  {
    "objectID": "cruise_reports/cruise_reports/SV18173.html",
    "href": "cruise_reports/cruise_reports/SV18173.html",
    "title": "SV18173",
    "section": "",
    "text": "SV18173\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 29 files for cruise SV18173 \nTotal rows: 1906 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\nerror generating surface map for chlorophyll_fluorescence:\n&lt;error/rlang_error&gt;\nError in `summarise()`:\nℹ In argument: `mean_value = mean(.data[[\"chlorophyll_fluorescence\"]],\n  na.rm = TRUE)`.\nℹ In group 1: `station = \"10\"`.\nCaused by error:\n! error in evaluating the argument 'x' in selecting a method for function 'mean': Column `chlorophyll_fluorescence` not found in `.data`.\n---\nBacktrace:\n     ▆\n  1. ├─global createSurfaceMap(...)\n  2. │ ├─base::tryCatch(...)\n  3. │ │ └─base (local) tryCatchList(expr, classes, parentenv, handlers)\n  4. │ │   └─base (local) tryCatchOne(expr, names, parentenv, handlers[[1L]])\n  5. │ │     └─base (local) doTryCatch(return(expr), name, parentenv, handler)\n  6. │ └─... %&gt;% ...\n  7. ├─dplyr::summarise(...)\n  8. ├─dplyr:::summarise.grouped_df(...)\n  9. │ └─dplyr:::summarise_cols(.data, dplyr_quosures(...), by, \"summarise\")\n 10. │   ├─base::withCallingHandlers(...)\n 11. │   └─dplyr:::map(quosures, summarise_eval_one, mask = mask)\n 12. │     └─base::lapply(.x, .f, ...)\n 13. │       └─dplyr (local) FUN(X[[i]], ...)\n 14. │         └─mask$eval_all_summarise(quo)\n 15. │           └─dplyr (local) eval()\n 16. ├─base::mean(.data[[\"chlorophyll_fluorescence\"]], na.rm = TRUE)\n 17. ├─chlorophyll_fluorescence\n 18. ├─rlang:::`[[.rlang_data_pronoun`(.data, \"chlorophyll_fluorescence\")\n 19. │ └─rlang:::data_pronoun_get(...)\n 20. ├─rlang:::abort_data_pronoun(x, call = y)\n 21. │ └─rlang::abort(msg, \"rlang_error_data_pronoun_not_found\", call = call)\n 22. │   └─rlang:::signal_abort(cnd, .file)\n 23. │     └─base::signalCondition(cnd)\n 24. └─base (local) `&lt;fn&gt;`(`&lt;rln_____&gt;`)\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS0603.html",
    "href": "cruise_reports/cruise_reports/WS0603.html",
    "title": "WS0603",
    "section": "",
    "text": "WS0603\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 92 files for cruise WS0603 \nTotal rows: 12980 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS0618.html",
    "href": "cruise_reports/cruise_reports/WS0618.html",
    "title": "WS0618",
    "section": "",
    "text": "WS0618\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 50 files for cruise WS0618 \nTotal rows: 9305 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS0704.html",
    "href": "cruise_reports/cruise_reports/WS0704.html",
    "title": "WS0704",
    "section": "",
    "text": "WS0704\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 34 files for cruise WS0704 \nTotal rows: 4948 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS0802.html",
    "href": "cruise_reports/cruise_reports/WS0802.html",
    "title": "WS0802",
    "section": "",
    "text": "WS0802\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 34 files for cruise WS0802 \nTotal rows: 5347 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS0901.html",
    "href": "cruise_reports/cruise_reports/WS0901.html",
    "title": "WS0901",
    "section": "",
    "text": "WS0901\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 39 files for cruise WS0901 \nTotal rows: 5032 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS0914.html",
    "href": "cruise_reports/cruise_reports/WS0914.html",
    "title": "WS0914",
    "section": "",
    "text": "WS0914\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 24 files for cruise WS0914 \nTotal rows: 4596 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS0923.html",
    "href": "cruise_reports/cruise_reports/WS0923.html",
    "title": "WS0923",
    "section": "",
    "text": "WS0923\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 38 files for cruise WS0923 \nTotal rows: 4408 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS1007.html",
    "href": "cruise_reports/cruise_reports/WS1007.html",
    "title": "WS1007",
    "section": "",
    "text": "WS1007\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 35 files for cruise WS1007 \nTotal rows: 4112 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS1018.html",
    "href": "cruise_reports/cruise_reports/WS1018.html",
    "title": "WS1018",
    "section": "",
    "text": "WS1018\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 31 files for cruise WS1018 \nTotal rows: 4136 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS1106.html",
    "href": "cruise_reports/cruise_reports/WS1106.html",
    "title": "WS1106",
    "section": "",
    "text": "WS1106\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 30 files for cruise WS1106 \nTotal rows: 4382 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS1116.html",
    "href": "cruise_reports/cruise_reports/WS1116.html",
    "title": "WS1116",
    "section": "",
    "text": "WS1116\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 24 files for cruise WS1116 \nTotal rows: 5115 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS1207.html",
    "href": "cruise_reports/cruise_reports/WS1207.html",
    "title": "WS1207",
    "section": "",
    "text": "WS1207\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 34 files for cruise WS1207 \nTotal rows: 4868 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS14335.html",
    "href": "cruise_reports/cruise_reports/WS14335.html",
    "title": "WS14335",
    "section": "",
    "text": "WS14335\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 31 files for cruise WS14335 \nTotal rows: 609 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS15148.html",
    "href": "cruise_reports/cruise_reports/WS15148.html",
    "title": "WS15148",
    "section": "",
    "text": "WS15148\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 27 files for cruise WS15148 \nTotal rows: 1801 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS15264.html",
    "href": "cruise_reports/cruise_reports/WS15264.html",
    "title": "WS15264",
    "section": "",
    "text": "WS15264\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 29 files for cruise WS15264 \nTotal rows: 1786 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS16004.html",
    "href": "cruise_reports/cruise_reports/WS16004.html",
    "title": "WS16004",
    "section": "",
    "text": "WS16004\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 27 files for cruise WS16004 \nTotal rows: 1760 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS16207.html",
    "href": "cruise_reports/cruise_reports/WS16207.html",
    "title": "WS16207",
    "section": "",
    "text": "WS16207\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 26 files for cruise WS16207 \nTotal rows: 1605 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS16319.html",
    "href": "cruise_reports/cruise_reports/WS16319.html",
    "title": "WS16319",
    "section": "",
    "text": "WS16319\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 28 files for cruise WS16319 \nTotal rows: 1577 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS17086.html",
    "href": "cruise_reports/cruise_reports/WS17086.html",
    "title": "WS17086",
    "section": "",
    "text": "WS17086\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 30 files for cruise WS17086 \nTotal rows: 1913 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS17212.html",
    "href": "cruise_reports/cruise_reports/WS17212.html",
    "title": "WS17212",
    "section": "",
    "text": "WS17212\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 24 files for cruise WS17212 \nTotal rows: 1844 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS18008.html",
    "href": "cruise_reports/cruise_reports/WS18008.html",
    "title": "WS18008",
    "section": "",
    "text": "WS18008\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 30 files for cruise WS18008 \nTotal rows: 1841 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS18218.html",
    "href": "cruise_reports/cruise_reports/WS18218.html",
    "title": "WS18218",
    "section": "",
    "text": "WS18218\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 27 files for cruise WS18218 \nTotal rows: 1765 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS18351.html",
    "href": "cruise_reports/cruise_reports/WS18351.html",
    "title": "WS18351",
    "section": "",
    "text": "WS18351\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 11 files for cruise WS18351 \nTotal rows: 1406 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS19119.html",
    "href": "cruise_reports/cruise_reports/WS19119.html",
    "title": "WS19119",
    "section": "",
    "text": "WS19119\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 44 files for cruise WS19119 \nTotal rows: 2387 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS19266.html",
    "href": "cruise_reports/cruise_reports/WS19266.html",
    "title": "WS19266",
    "section": "",
    "text": "WS19266\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 63 files for cruise WS19266 \nTotal rows: 5186 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS20006.html",
    "href": "cruise_reports/cruise_reports/WS20006.html",
    "title": "WS20006",
    "section": "",
    "text": "WS20006\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 50 files for cruise WS20006 \nTotal rows: 3917 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS20279.html",
    "href": "cruise_reports/cruise_reports/WS20279.html",
    "title": "WS20279",
    "section": "",
    "text": "WS20279\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 66 files for cruise WS20279 \nTotal rows: 4836 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS21032.html",
    "href": "cruise_reports/cruise_reports/WS21032.html",
    "title": "WS21032",
    "section": "",
    "text": "WS21032\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 68 files for cruise WS21032 \nTotal rows: 4704 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS21151.html",
    "href": "cruise_reports/cruise_reports/WS21151.html",
    "title": "WS21151",
    "section": "",
    "text": "WS21151\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 75 files for cruise WS21151 \nTotal rows: 5191 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS21278.html",
    "href": "cruise_reports/cruise_reports/WS21278.html",
    "title": "WS21278",
    "section": "",
    "text": "WS21278\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 65 files for cruise WS21278 \nTotal rows: 5002 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS22022.html",
    "href": "cruise_reports/cruise_reports/WS22022.html",
    "title": "WS22022",
    "section": "",
    "text": "WS22022\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 60 files for cruise WS22022 \nTotal rows: 4375 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS22141.html",
    "href": "cruise_reports/cruise_reports/WS22141.html",
    "title": "WS22141",
    "section": "",
    "text": "WS22141\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 66 files for cruise WS22141 \nTotal rows: 4778 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS22337.html",
    "href": "cruise_reports/cruise_reports/WS22337.html",
    "title": "WS22337",
    "section": "",
    "text": "WS22337\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 79 files for cruise WS22337 \nTotal rows: 6113 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS23061.html",
    "href": "cruise_reports/cruise_reports/WS23061.html",
    "title": "WS23061",
    "section": "",
    "text": "WS23061\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 86 files for cruise WS23061 \nTotal rows: 6690 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  },
  {
    "objectID": "cruise_reports/cruise_reports/WS24139.html",
    "href": "cruise_reports/cruise_reports/WS24139.html",
    "title": "WS24139",
    "section": "",
    "text": "WS24139\n\n\nsetup\nif (!nzchar(system.file(package = \"librarian\"))) {\n  install.packages(\"librarian\")\n}\n\nlibrarian::shelf(\n  quiet = TRUE,\n  readr, here, fs, ggplot2, glue, \"jiho/castr\", dplyr, oce, patchwork, purrr, tidyr\n)\n\n\n\n\nCode\nsource(here::here(\"R/load_cruise_ctds.R\"))\ncruise_df &lt;- load_cruise_ctds(params$cruise_id)\n\n\nLoaded 86 files for cruise WS24139 \nTotal rows: 6968 \n\n\n\n\nplot across all stations\nlibrary(plotly)\n\np &lt;- ggplot(cruise_df, aes(x = time, y = sea_water_temperature, color = station)) +\n  geom_point(size = 1.8) +\n  labs(x = \"Time\", y = \"Temperature (°C)\",\n       title = \"Temperature over Time by Station\") +\n  theme_minimal()\n\nggplotly(p)\n\n\n\n\n\n\n\n\ninteractive map of stations\nlibrary(leaflet)\n\n# Get one location per station (taking the first non-NA lat/lon for each station)\nstation_locations &lt;- cruise_df %&gt;%\n  group_by(station) %&gt;%\n  summarise(\n    latitude = first(na.omit(latitude)),\n    longitude = first(na.omit(longitude)),\n    n_observations = n(),\n    mean_temp = mean(sea_water_temperature, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  filter(!is.na(latitude) & !is.na(longitude))\n\n# Create color palette based on temperature\npal &lt;- colorNumeric(\n  palette = \"RdYlBu\",\n  domain = station_locations$mean_temp,\n  reverse = TRUE  # Red for warm, blue for cold\n)\n\n# Create interactive map\nleaflet(station_locations) %&gt;%\n  addTiles() %&gt;%  # Add default OpenStreetMap tiles\n  addCircleMarkers(\n    lng = ~longitude,\n    lat = ~latitude,\n    radius = 8,\n    color = \"#333333\",\n    fillColor = ~pal(mean_temp),\n    fillOpacity = 0.8,\n    weight = 1,\n    popup = ~paste0(\n      \"&lt;strong&gt;Station: &lt;/strong&gt;\", station, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Observations: &lt;/strong&gt;\", n_observations, \"&lt;br&gt;\",\n      \"&lt;strong&gt;Mean Temp: &lt;/strong&gt;\", round(mean_temp, 2), \" °C&lt;br&gt;\",\n      \"&lt;strong&gt;Lat: &lt;/strong&gt;\", round(latitude, 4), \"&lt;br&gt;\",\n      \"&lt;strong&gt;Lon: &lt;/strong&gt;\", round(longitude, 4)\n    ),\n    label = ~station\n  ) %&gt;%\n  addLegend(\n    position = \"bottomright\",\n    pal = pal,\n    values = ~mean_temp,\n    title = \"Mean Temperature (°C)\",\n    opacity = 0.8\n  ) %&gt;%\n  addScaleBar(position = \"bottomleft\")\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap &lt;- function(df, var_name, legend_min, legend_max){\n  # additional: parameters\n  # model = try \"Sph\" (spherical), \"Exp\" (exponential) or \"Gau\" (Gaussian) to get the best variogram fit\n  MODEL &lt;- \"Exp\"\n  # length.out Increase for a finer grid (slower), decrease for speed\n  LENGTH_OUT = 300\n  # bbox_ext = 0.1 Adjust padding around your data extent (in degrees)\n  #A nugget = 0 Set to a small positive value if data has measurement error\n\n  output &lt;- tryCatch({\n    # get average value of var_name for 0-5 meters in df\n    df &lt;- df %&gt;%\n      filter(depth &gt;= 0 & depth &lt;= 5) %&gt;%\n      group_by(station) %&gt;%\n      summarise(\n        mean_value = mean(.data[[var_name]], na.rm = TRUE), \n        latitude   = first(na.omit(latitude)),\n        longitude  = first(na.omit(longitude)),\n        .groups    = \"drop\"\n      )\n  \n    librarian::shelf(\n      quiet = TRUE,\n      sp, gstat, ggplot2, viridis, raster, sf\n    )\n  \n    # --- 1. Prepare spatial data ---\n    df_sp &lt;- df\n    df_sp$latitude  &lt;- as.numeric(df_sp$latitude)\n    df_sp$longitude &lt;- as.numeric(df_sp$longitude)\n    df_sp &lt;- df_sp[!is.na(df_sp$mean_value) & !is.na(df_sp$latitude) & !is.na(df_sp$longitude), ]\n    coordinates(df_sp) &lt;- ~longitude + latitude\n    proj4string(df_sp) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n  \n    # --- 2. Create interpolation grid ---\n    bbox_ext  &lt;- 0.1\n    lon_range &lt;- seq(bbox(df_sp)[1,1] - bbox_ext, bbox(df_sp)[1,2] + bbox_ext, length.out = LENGTH_OUT)\n    lat_range &lt;- seq(bbox(df_sp)[2,1] - bbox_ext, bbox(df_sp)[2,2] + bbox_ext, length.out = LENGTH_OUT)\n  \n    grid &lt;- expand.grid(longitude = lon_range, latitude = lat_range)\n    coordinates(grid) &lt;- ~longitude + latitude\n    proj4string(grid) &lt;- CRS(\"+proj=longlat +datum=WGS84\")\n    gridded(grid) &lt;- TRUE\n  \n    # --- 3. Fit variogram ---\n    vgm_emp &lt;- variogram(mean_value ~ 1, data = df_sp)\n    vgm_fit &lt;- fit.variogram(vgm_emp, model = vgm(\n      psill  = max(vgm_emp$gamma),\n      model  = MODEL,\n      range  = max(vgm_emp$dist) / 2,\n      nugget = min(vgm_emp$gamma)\n    ))\n  \n    vgm_line &lt;- variogramLine(vgm_fit, maxdist = max(vgm_emp$dist))\n  \n    print(\n      ggplot() +\n        geom_point(data = vgm_emp, aes(x = dist, y = gamma, size = np), color = \"steelblue\") +\n        geom_line(data = vgm_line, aes(x = dist, y = gamma), color = \"red\", linewidth = 1) +\n        labs(title = \"Empirical Variogram with Fitted Model\",\n             x = \"Distance\", y = \"Semivariance\", size = \"Num. pairs\") +\n        theme_minimal(base_size = 13)\n    )\n  \n    # --- 4. Perform Ordinary Kriging ---\n    krig_result &lt;- suppressWarnings(krige(\n      mean_value ~ 1, \n      locations = df_sp, \n      newdata = grid, \n      model = vgm_fit\n    ))\n  \n    # --- 5. Convert to data frame and clip to convex hull ---\n    krig_df &lt;- as.data.frame(krig_result)\n    names(krig_df)[names(krig_df) == \"var1.pred\"] &lt;- \"predicted\"\n    names(krig_df)[names(krig_df) == \"var1.var\"]  &lt;- \"variance\"\n  \n    # Build convex hull from sample points\n    pts_sf  &lt;- st_as_sf(df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    hull_sf &lt;- st_convex_hull(st_union(pts_sf))\n  \n    # Clip kriged grid to hull\n    krig_sf      &lt;- st_as_sf(krig_df, coords = c(\"longitude\", \"latitude\"), crs = 4326)\n    krig_clipped &lt;- st_filter(krig_sf, hull_sf)\n    krig_df      &lt;- cbind(st_drop_geometry(krig_clipped), st_coordinates(krig_clipped))\n    names(krig_df)[names(krig_df) == \"X\"] &lt;- \"longitude\"\n    names(krig_df)[names(krig_df) == \"Y\"] &lt;- \"latitude\"\n  \n    # --- 6. Plot surface map ---\n  librarian::shelf(quiet = TRUE, ggplot2, viridis, sf, ggspatial, rnaturalearth, rnaturalearthdata)\n  \n  # --- Basemap ---\n  world &lt;- ne_countries(scale = \"medium\", returnclass = \"sf\")\n  \n    print(\n      ggplot() +\n        # Kriged surface\n        geom_raster(data = krig_df, aes(x = longitude, y = latitude, fill = predicted), interpolate = TRUE) +\n        geom_point(data = df, aes(x = longitude, y = latitude, fill = mean_value),\n                   shape = 21, color = \"black\", size = 2.5) +\n        scale_fill_viridis_c(\n          option = \"plasma\",\n          name   = \"Value\",\n          limits = c(legend_min, legend_max),\n          oob    = scales::squish\n        ) +\n        # Basemap\n        geom_sf(data = world, fill = \"gray85\", color = \"gray50\", linewidth = 0.3) +\n  \n        # Colorscale with hardcoded limits\n        scale_fill_viridis_c(\n          option  = \"plasma\",\n          name    = \"Value\",\n          limits  = c(legend_min, legend_max),\n          oob     = scales::squish   # squish values outside limits into the color range\n        ) +\n        # Zoom to data extent with a small buffer\n        coord_sf(\n          xlim = c(min(krig_df$longitude) - 0.1, max(krig_df$longitude) + 0.1),\n          ylim = c(min(krig_df$latitude)  - 0.1, max(krig_df$latitude)  + 0.1),\n          expand = FALSE\n        ) +\n        annotation_scale(location = \"bl\") +\n        annotation_north_arrow(location = \"tr\", style = north_arrow_fancy_orienteering()) +\n        labs(\n          title = paste(\"Kriging Surface:\", var_name),\n          x = \"Longitude\", y = \"Latitude\"\n        ) +\n        theme_minimal(base_size = 13) +\n        theme(axis.text.x = element_text(angle = 45, hjust = 1))\n    )\n  },\n  error = function(e) {\n    print(glue::glue(\"error generating surface map for {var_name}:\"))\n    print(e)\n  })\n}\n\ncreateSurfaceMap(cruise_df, \"sea_water_temperature\", 15, 30)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"chlorophyll_fluorescence\", 0.02, 0.1)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"oxygen_saturation\", 4, 7)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]\n\n\n\n\n\n\n\n\n\nCode\ncreateSurfaceMap(cruise_df, \"sea_water_salinity\", 30, 40)\n\n\n\n\n\n\n\n\n\n[using ordinary kriging]"
  }
]